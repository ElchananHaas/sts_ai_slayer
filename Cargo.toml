[package]
name = "sts_ai_slayer"
version = "0.1.0"
edition = "2024"

[dependencies]
rand_chacha = { version = "0.9.0", features = ["os_rng"] }
strum = { version = "0.27.1", features = ["derive", "strum_macros"] }
